## 1. Implementation
- [ ] 1.1 Language-neutral scenario table under conformance/: scenario name, scripted server behavior, expected client outcome — the single source all four suites implement (CLT-090)
- [ ] 1.2 Scripted counterpart server harness: per-scenario behaviors (permuted replies, oversize frame, garbage frame, unknown-id reply, push injection, delayed reply, connection drop) reusable from every language's test runner
      — PARTIAL: every behavior in the list is scriptable in all four languages today, but from four independent ad-hoc harnesses (rust/thunder/tests/behavior.rs free helpers; typescript MockServer/ServerConn classes; python/tests/mockserver.py; csharp/HiveLLM.Thunder.Tests/MockServer.cs), not from one shared source. Left open deliberately: closing it means 1.1 first, then rewriting all four suites to drive from it.
- [x] 1.3 Pipelined out-of-order completion: N concurrent calls, replies permuted by the server, every pending call resolves with its own id's result
      — five_pipelined_calls_complete_in_permuted_order x4 (reply order [2,0,4,1,3] — neither submission order nor its reverse). The pre-existing N=2 test is kept: with N=2 "permuted" and "reversed" are the same thing, so it could not distinguish id-routing from arrival-order pairing. Mutation-checked in Rust: mis-pairing id and value fails it.
- [x] 1.4 Oversize refusal: inbound frame with declared length over the cap refused WITHOUT allocating the body; typed frame-too-large error; connection poisoned
      — the typed error + poisoning were already covered x4. The ALLOCATION half was not, and its shape is per-language: only Rust pre-sizes the body from the prefix (`vec![0u8; len]` in wire/frame.rs read_frame), so it gets a real probe — rust/thunder/tests/oversize_alloc.rs, a counting #[global_allocator] in its own test binary, asserting a frame declaring 512 MiB allocates < 8 MiB. Mutation-checked: moving the allocation above the cap check fails it with 536,888,245 bytes. TS/Python/C# accumulate-then-split (splitFrame / try_split_frame / TryDecodeRequest) and never size a buffer from the declared length, so there is nothing to probe; the prefix-only refusal is pinned for them by corpus vector framing-cap-plus-one, which all four run.
- [x] 1.5 Connect timeout and call timeout scenarios fire as typed timeout-class errors at the configured deadlines
      — call timeout was already covered x4. Connect timeout was untested in ALL FOUR despite the config field being wired; now connect_timeout_fires_as_typed_timeout x5 (Python sync + async). Dials TEST-NET-1 192.0.2.1:9 (RFC 5737, routable nowhere) so the SYN is dropped rather than refused — a closed localhost port is refused instantly and would be the connection class, not this one. Asserts both the typed class and that the full timeout elapsed.
- [x] 1.6 Reconnect: 2 attempts then typed connection failure; successful reconnect replays the profile handshake before pending traffic
      — the 2-attempts-then-typed-failure half was already covered x4. The REPLAY half was asserted nowhere: the successful-reconnect tests ran on Handshake::None, which has no handshake to replay, and the give-up test slams the re-dial shut before any HELLO is read. Now successful_reconnect_replays_the_handshake_before_pending_traffic x4 records what the re-dialed connection actually receives and asserts it is exactly ["HELLO", "B"].
- [x] 1.7 Push routing per profile: push = Reserved receiving a PUSH_ID frame is a protocol error + poison; push = Enabled delivers the decoded Value to the registered hook (CLT-060)
      — already covered x4, both halves; item was never ticked.
- [x] 1.8 Error-class mapping for both conventions: NOAUTH/WRONGPASS map to the auth class regardless of convention (CLT-051); "[code] " bracket-code parsed into code + class (CLT-050); assertions branch on class/code, never message text (CLT-052)
      — the WRONGPASS-over-the-wire hole is closed. Both wire error-mapping tests now also send WRONGPASS: under the RESP3-prefix convention (resp3_error_mapping_over_the_wire) AND under the bracket-code convention (bracket_error_mapping_over_the_wire), asserting the auth class each time — that is the "regardless of convention" half of CLT-051, which the unit table pinned but no socket test did. x4 (Python sync + async). Confirmed against the impl that under BracketCode the auth-prefix check runs after the "[code]" is stripped (client/error.rs starts_with_auth_prefix on the remainder).
- [x] 1.9 Unknown-id drop (response for an id never sent is discarded without disturbing pending calls) and poison-on-malformed-frame scenarios
      — already covered x4; item was never ticked.
- [x] 1.10 Wire the suite into the default test run of all four languages; same scenarios, same assertions — green x4 is gate G4
      — all of the above run in each language's default test command, no gating flags. Green x4 as of this pass: Rust 104 (clippy clean), TypeScript 118 (tsc + eslint clean), Python 133 (ruff clean), C# 122.
- [ ] 1.11 Live interop smoke per TST-050: env-gated on THUNDER_LIVE_URL_SYNAP/NEXUS/VECTORIZER — connect, handshake per profile, PING-class call, one typed-error call, clean close, per language; skipped when unset, exercised on the release path
      — NOT STARTED, and blocked on a decision, not on effort. TST-050 was written when Thunder had a per-product Profile registry; the agnostic-config owner directive (ec6be5a, "nada de profiles nominais") deleted that registry — no product name survives in any library. "Handshake per profile" now has no profile to name: a live smoke must build the per-product shape (Synap = auth_command, Nexus = arg-less HELLO, none run TLS — BN-023, docs/analysis/behavioral-normalization/03-blockers-by-product.md) in the TEST, keyed off which env var is set, which reintroduces product knowledge into the test tree. That is a boundary call for the owner, not a mechanical write. Also unverifiable here: no live products in this env, so only compile + skip-when-unset could be checked, never the actual connect/handshake. Owner decision (2026-07-17): stop phase4 here; re-spec TST-050 against the agnostic-config model before implementing, as its own task. The shape-named builders it would reuse (argless_hello_config / auth_command_config / hello_mandatory_config) already exist in all four languages.

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [ ] 2.1 Update or create documentation covering the implementation
- [x] 2.2 Write tests covering the new behavior
- [x] 2.3 Run tests and confirm they pass
