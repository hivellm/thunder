## 1. Implementation
- [x] 1.1 Value group: every variant alone and nested; empty Bytes/Str/Array/Map; Map with non-string keys; i64::MIN/MAX and compact-int boundaries (-32, 127, 255, 65535, ...); f64 NaN bit pattern, ±∞, -0.0; Err plain, Err with "[code] " prefix, NOAUTH/WRONGPASS strings (TST-011)
- [x] 1.2 Framing group completion: two frames in one buffer, partial header, partial body, zero-length body, frame at exactly the cap, frame one byte over the cap as reject with no-allocation assertion (TST-012) — at-cap/over-cap use the schema's `max_frame_bytes` override; the default-cap `framing-cap-plus-one` vector is prefix-only, so allocation is impossible by construction
- [x] 1.3 Tolerance group (decode-only): Bytes as int-array (Synap legacy) and map-shaped Request (TST-013); push group vector with id = u32::MAX (TST-014) — legacy bytes generated by the legacy encoders themselves (nexus-protocol / to_vec_named), never hand-computed
- [x] 1.4 Handshake group: Nexus HELLO [Int(1)] positional request/reply shape; Vectorizer HELLO map (version/token/api_key/client_name) and capabilities reply (TST-015)
- [x] 1.5 Rust corpus loader (~50 LOC) walking conformance/vectors/ and asserting per mode (bidirectional/decode-only/reject), wired into the DEFAULT test run - no feature gates, no #[ignore] (TST-020, NFR-03) — extended: full node types (array/map/float `bits`), structural decode comparison with bit-pattern floats, `stream` mode, `max_frame_bytes`, decode-only now also asserts encode(decoded) != legacy frame
- [x] 1.6 Reference cross-decode: nexus-protocol as dev-dependency, both directions over the canonical + value groups - Thunder frames decode via nexus_protocol::rpc into equal structures and vice versa (TST-030) — byte equality asserted for all non-Bytes trees; Bytes is the one deliberate asymmetry (Thunder emits bin per WIRE-010, the reference still emits legacy seq), pinned by a dedicated test so the exemption can't rot
- [x] 1.7 Pairwise-fuzz seed generator: deterministic per seed, random Value trees as JSON for encode/decode/re-encode agreement; auto-shrink divergences to the shortest failing tree and graduate fixes into new corpus vectors (TST-040, TST-041) — Rust lane landed: checked-in LCG generator (no rand dep, algorithm documented as the normative spec for other lanes), 200 seeds, round-trip + re-encode byte-stability; cross-language pairwise comparison and TST-041 auto-shrink activate when the other language lanes exist (Phase 1 is Rust-only)
- [ ] 1.8 CI lanes: corpus per PR + nightly rolling-seed fuzz (TST-021, TST-040) — needs the remote/CI workflow setup; corpus + fuzz already run in the default `cargo test`, so wiring is a workflow file away
- [x] 1.9 Coverage check: every SPEC-001 MUST maps to ≥1 vector, recorded in the vector's notes field (TST-016) — WIRE-001..005, 010..016, 020..023, 040 all appear in vector notes; WIRE-030/031 (layer purity, library pins) are code-review/CI properties with no byte representation, not vector-mappable

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — conformance/README.md documents the extended schema (`stream` mode, `frames`, `max_frame_bytes`, float `bits`, generated-hex provenance rule)
- [x] 2.2 Write tests covering the new behavior — corpus.rs (rewritten loader), cross_decode.rs (7 tests), fuzz_seeds.rs (2 tests)
- [x] 2.3 Run tests and confirm they pass — thunder-wire: fmt + clippy -D warnings + full test suite green (38 corpus vectors)
