## 1. Implementation
- [x] 1.1 `conformance/profiles/synap.yaml`: `handshake: none` → `auth_command`, keep `hello_style: null`; update the inline comment to note `require_auth`-gated AUTH, no HELLO handler (BN-017/BN-023)
- [x] 1.2 `conformance/profiles/nexus.yaml`: correct `hello_style` from `positional_version` to the arg-less/metadata-reply form; fix comment (positional `[Int(1)]` is RESP3, not RPC)
- [x] 1.3 `conformance/profiles/vectorizer.yaml`: `tls: optional_rustls` → `reserved_config` (spec-only, unwired); quote all `tls` scalars (`"off"`) across the four YAMLs so YAML-1.1 loaders don't coerce to bool
- [x] 1.4 `conformance/vectors/handshake-nexus-hello-request.yaml`: replace the RESP3 positional request with the actual RPC arg-less HELLO shape — bytes regenerated from the reference encoder (`09 00 00 00 93 01 a5 48 45 4c 4c 4f 90`), never hand-computed
- [x] 1.5 Add `NOPERM` to the recognized auth-family tokens in SPEC-002/SPEC-003 and confirm each language classifier maps it to the Auth class — all four already classified it (rust error.rs, ts errors.ts, py errors.py, cs Errors.cs); added the family-pinned `NOPERM` constant to the Rust server alongside NOAUTH/WRONGPASS
- [x] 1.6 Regenerate `Profile` constants + profile-pinning tests: rust/thunder (src/wire/profile.rs, tests/profiles.rs), typescript (profile.ts + test), python (profile.py + test — PyYAML `off`→bool workaround removed now that the YAML is quoted), csharp (Profile.cs + test)
- [x] 1.7 Update SPEC-002 PRO-001 prose (+ new normative PRO-001a: handshake shape ≠ auth policy) and PRO-011 registry table; confirm the `synap` profile now drives an AUTH-capable handshake for credentialed deployments
- [x] 1.8 **Follow-on the errata forced** (not in the original scope): the server derived auth *enforcement* from the handshake *shape*, so the corrected `synap` profile would have made every deployment closed — as wrong as the old cell made them all open. Added `ListenerConfig::auth_required` (default `true`, `.open()` to disable), mirroring Nexus's `auth_required` / Synap's `require_auth`; recorded normatively in SPEC-004 SRV-011.

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — SPEC-002 PRO-001/PRO-001a/PRO-011, SPEC-003 CLT-051, SPEC-004 SRV-011, profile YAML comments, root/python/typescript/csharp READMEs; BN-023 marked RESOLVED in the analysis
- [x] 2.2 Write tests covering the new behavior — Rust: `synap_profile_sends_auth_and_never_hello`, `synap_profile_without_credentials_sends_nothing`, `synap_profile_gates_until_auth_when_the_deployment_requires_it`, `auth_command_profile_serves_uncredentialed_sessions_when_open`, `open_deployment_still_rejects_bad_credentials`; mirrored per language in TS/Python(×2 clients)/C#; profile pins assert the corrected cells
- [x] 2.3 Run tests and confirm they pass — full gate green in all four languages, verified independently of the agents: rust 126 (fmt + clippy -D warnings clean), python 123 (ruff + black clean from root), typescript 109 (typecheck + lint clean), csharp 116 (build -warnaserror clean)
