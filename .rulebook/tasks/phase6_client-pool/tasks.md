## 1. Implementation
- [x] 1.1 Settle the spec — CLT-080 says "MAY" and is [P1]
      — OWNER DECISION 2026-07-17: promote to **[P0] "SHALL, in every language."** A per-language subset is the worst outcome; all-four-or-unbuilt. Ratified in SPEC-003 §8.
- [x] 1.2 De-stale SPEC-003 §8 while it is open (CLT-070/071 route through the deleted profile registry; the pool takes a Config, no registry/scheme-table/product-name)
      — CLT-080 rewritten as the [P0] contract (fixed N, semaphore, lazy connect, checkout/return guard, no external crate, takes Config+ClientConfig, no product name). CLT-070/071 de-staled too: they still said "every registered profile scheme" / "the profile registry" — now aligned with PRO-012 (parser takes the caller's Config; a scheme Thunder never heard of works because the app configured it).
- [x] 1.3 Read the four donors; extract shared shape vs language idiom; record what is dropped and why
      — read Vectorizer/sdks/rust/src/rpc/pool.rs (149 LOC): RpcPoolConfig + Semaphore permits + idle Vec + acquire() (pop idle or dial) + PooledClient Drop-returns. The one Thunder adaptation: the handshake lives inside Client::connect_with, so reusing the Client amortizes it (no separate hello() call). bb8/deadpool rejection inherited consciously (async traits + reconnect logic this layer doesn't need). The donor relies on call-time errors for dead connections; we go further per 1.6 (drop on return).
- [x] 1.4 Implement in Rust first as the reference: client/pool.rs behind the `client` feature
      — Pool + PooledConn. Fixed max_connections (Semaphore, clamped >=1), idle Vec, lazy connect on first checkout, RAII Drop returning the connection. Takes Config+ClientConfig, builds the same Client as connect_with — no wire behavior of its own. Added Client::is_alive() (public) so the pool can drop poisoned connections. Exported as thunder::{Pool, PooledConn} and thunder::client::Pool.
- [x] 1.5 Port to TypeScript, Python and C# from the Rust reference (same knobs/semantics, guard per idiom)
      — DONE, all three: TypeScript (f7de9ce) — Pool + PooledConn guard with explicit release(); Python (a7398db) — sync Pool + async AsyncPool, guard as a context manager (with / async with); C# (d28bcf1) — Pool + PooledConn guard as IAsyncDisposable (await using). Each: fixed max_connections (semaphore), lazy connect, idle reuse, drop-poisoned-on-return, and the N-ops-one-handshake test. Gates green (vitest 127, pytest 148, dotnet 131).
- [x] 1.6 Hold the layering invariants (CLT-001 API unchanged; CLT-012 per-connection; CLT-014 poisoned dropped; CLT-030 owns reconnect; no health checks / reaping / warmup)
      — Client API untouched (pool is additive). max_in_flight stays per-connection (the pool bounds connections, not in-flight calls). A poisoned/closed connection is dropped on return AND skipped if it died while idle; the next checkout dials fresh. No health checks / background reaping / min-idle warmup.
- [x] 1.7 Prove the mechanism: N sequential operations → one connect + one handshake, not N
      — tests/pool.rs n_operations_use_one_connection_and_handshake: a counting Dispatch records distinct Session::connection_id()s; 10 pooled operations produce exactly 1 distinct connection (hence one HELLO handshake under the standard profile). Plus checkout/return, the max_connections cap (a 3rd checkout blocks while 2 are held, then proceeds on release), and poison-not-returned.
- [x] 1.8 Make no quantitative claim (BEN-031)
      — none made. The module and spec state the mechanism (one handshake for N ops) and cite the two root-caused failures (Nexus per-request REST, Vectorizer per-RPC Raft, TIME_WAIT), never a throughput number. The family's unbacked "80%" figure is not inherited.

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation — SPEC-003 §8 rewritten (1.1/1.2); pool.rs has module docs with a usage example. The "swap note" (product-side pool stopgap) was REMOVED from CLT-080 — it is false once the pool ships. Per-language README pool sections land with 1.5.
- [x] 2.2 Write tests — checkout/return, cap never exceeded under concurrent checkout, the guard returns on the error path (RAII Drop runs regardless), a poisoned connection is not handed to the next caller, and the one-handshake-for-N test. (Rust; the other three languages' tests ship with 1.5.)
- [x] 2.3 Run tests and confirm they pass — cargo test -p thunder-rpc green (60 lib incl. the pool unit test + 4 pool integration tests), clippy clean, fmt clean. Conformance corpus unchanged — the pool is above the wire and moved no vector.
