## 1. Implementation
- [x] 1.1 Draft `docs/specs/SPEC-008-canonical-behavior.md` skeleton (one section per dimension; requirement IDs e.g. `CAN-001..`), cross-linked to SPEC-001..005 and the behavioral-normalization analysis (BN-xxx)
      — SPEC-008 written in house style (metadata block, CAN- prefix, RFC-2119 + [P0]/[P1] tags), 8 sections. Added to the docs/specs/README.md index table. IMPORTANT reconciliation: the analysis predates the agnostic-config refactor (per-product profile registry deleted), so the canonical values are already committed in conformance/standard.yaml + SPEC-002 PRO-011 — SPEC-008 CITES them rather than re-deriving a "profile flip".
- [x] 1.2 Handshake section: pin the canonical HELLO-map shape as an optional opt-in capability; the shape ≠ auth-policy principle as a normative requirement; server dual-accept
      — CAN-010 (canonical HELLO-map shape, cites PRO-011/WIRE-004), CAN-011 (optional, opt-in; shape vs enforcement not conflated), CAN-012 (dual-accept). shape≠policy is CAN-002, citing PRO-001a/SRV-011 as its normative home (already committed there — SPEC-008 governs, does not supersede). KEY correction vs the proposal: the non-standard first frames (no-HELLO+AUTH, arg-less HELLO) are SUPPORTED CONFIGS under agnostic-config, not deprecated legacy — they stay bidirectional corpus vectors, NOT demoted to decode-only.
- [x] 1.3 TLS section: one optional config/feature-gated tokio-rustls layer, off by default, no STARTTLS; config keys; first running RPC TLS
      — CAN-020, cites SRV-040/FR-29 and the SRV-040-ordering decision note. Purely additive (off by default cannot break a plaintext client).
- [x] 1.4 Error-grammar section: pin the `[CODE] message` superset spelling and the recognized token set incl. NOPERM; declare the legacy grammars decode-only
      — CAN-030/031/032. THE open BN-011 decision is DECIDED: bare auth tokens {NOAUTH,WRONGPASS,NOPERM,NOPROTO} alongside bracketed [CODE], clients parse both. Correction vs proposal: the two prior grammars are SUBSETS of `both`, not "decode-only legacy" — and no product changes emission (bare tokens already emitted). NOPERM joins the recognized set.
- [x] 1.5 Caps + push sections: 64 MiB configurable default (pre-alloc check), single configurable max_in_flight; PUSH_ID server→client-only, uniform hook, emission = capability
      — CAN-040 (caps, cites WIRE-020/PRO-003), CAN-050 (push, cites CLT-060). Both cite the already-committed standard values.
- [x] 1.6 Add canonical corpus vectors: HELLO request + capabilities reply, `[CODE]` error forms (incl. NOPERM)
      — HELLO request/reply and [CODE]/NOAUTH/WRONGPASS vectors ALREADY existed (bidirectional). The one genuinely missing canonical vector, response-err-noperm.yaml, added — frame_hex generated from the reference encoder (throwaway test, deleted), round-trips bidirectional in all four language corpus loaders.
- [~] 1.7 Add legacy-tolerance corpus vectors (decode/accept-only): no-HELLO connect, arg-less HELLO, bare NOAUTH/WRONGPASS, 512 MiB cap config
      — RECONCILED with agnostic-config, mostly N/A: arg-less HELLO and bare auth tokens are VALID emittable configs now (bidirectional vectors already present), not decode-only legacy; "no-HELLO connect" and "512 MiB cap" are config values, not wire frames, so they are not corpus-vector-shaped. The genuinely-legacy decode-only forms (int-array Bytes, map-shaped Request) already exist (WIRE-011/016). CAN-090 documents this distinction. No spurious decode-only vectors added — that would misrepresent supported configs as deprecated.
- [x] 1.8 Record the one open spelling decision as DECIDED in the spec + a `.rulebook/decisions/` note
      — CAN-030 in the spec + .rulebook/decisions/2026-07-17-canonical-error-grammar.md with the two candidates, the decision (bare tokens + brackets), the rationale (least churn, RESP3 compat, no client-parse simplification from bracketing), and the rejected alternative. Owner can override; the wire is untouched either way.

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — SPEC-008 authored, indexed in docs/specs/README.md, and back-referenced from SPEC-002 (PRO-001a→CAN-002), SPEC-003 (CLT-050→CAN-030/031), SPEC-004 (SRV-040→CAN-020).
- [x] 2.2 Write tests covering the new behavior — response-err-noperm.yaml (bidirectional) loaded by all four corpus loaders; the existing canonical HELLO/error vectors already assert the shapes SPEC-008 pins.
- [x] 2.3 Run tests and confirm they pass — cargo test -p thunder-rpc green (corpus + full suite), clippy clean; corpus also green in Python (40), TypeScript (40), C# (40); anti-shrink floor bumped 38→39 in all four loaders.
