{
  "name": "@hivehub/thunder",
  "version": "0.1.0",
  "description": "HiveLLM binary RPC \u2014 wire codec + multiplexed client (skeleton; implementation lands at DAG T3.1)",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "node -e \"console.log('skeleton - tests land at T3.1')\"",
    "test:coverage": "node -e \"console.log('skeleton - coverage lands at T3.1')\""
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}