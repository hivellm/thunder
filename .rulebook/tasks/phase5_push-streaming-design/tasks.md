## 1. Implementation
- [x] 1.1 Inventory Synap's shipped push semantics — SUBSCRIBE flow, frame shapes, delivery guarantees — as the compatibility baseline (CLT-060/CLT-061 precedent)
      — docs/analysis/push-streaming/01-synap-subscribe-baseline.md (PUSH-001..PUSH-009). SUBSCRIBE is a catalog command; each pushed frame is Response{id=PUSH_ID, Ok(bare Value)} (Synap server.rs:295-298), delivered verbatim to the client hook (CLT-060). Baseline carries no event type, no subscription correlation, no stream termination — the three gaps §2 turns into requirements.
- [x] 1.2 Collect family push/streaming needs beyond SUBSCRIBE (candidate use cases across Nexus/Vectorizer/Lexum: watch, progress, invalidation)
      — 02-family-needs.md (PUSH-010..PUSH-016). watch/progress/invalidation + the SUBSCRIBE degenerate case collapse to one envelope of {stream, kind, data}; no use case needs new framing (PUSH-014) or Thunder to parse the payload (PUSH-015); backpressure explicitly out (PUSH-016).
- [ ] 1.3 Coordinate frame semantics with Synap — the only product shipping push — before specifying; record the agreement
      — OPEN, real blocker. Agreement surface recorded in 05-coordination-and-open-questions.md (PUSH-050..PUSH-053: envelope key names, does SUBSCRIBE migrate or coexist, reserved kind vocabulary, request-id-reuse correlation). This is an owner/cross-repo decision (product-swap policy, Thunder-repo work only) and cannot be settled in this repo. The proposal is deliberately shaped so Synap needs zero change to keep working (PUSH-035), which is what makes bringing it to coordination low-risk. The task's normative half (promoting the amendment + vectors) waits on this — see the promotion checklist in §5.
- [x] 1.4 Draft the SPEC-001 §push amendment: push/streaming frame semantics over the reserved PUSH_ID (id = u32::MAX), no wire version bump (WIRE-004/WIRE-005)
      — 03-spec-001-push-amendment.md (proposed WIRE-050..056). Canonical envelope Map{stream, kind, data} inside the existing Ok(Value); stream reuses the request-id space (WIRE-051), stream=0 is broadcast (WIRE-052), five reserved kinds with terminal end/error (WIRE-053/054), data opaque (WIRE-055), profile-gated not byte-gated (WIRE-056). Informative CLT-06x/SRV-01x sketches for the eventual client/server rules.
- [x] 1.5 Draft the PRO-001 `push` field evolution: how the profile dimension grows beyond Reserved | Enabled with backward-compatible defaults (existing profiles unchanged)
      — 04-pro-001-push-field-evolution.md (PUSH-030..035). Adds one enum case `Streaming` (strict superset of Enabled); default stays Reserved so it is a minor release (PRO-002/PRO-022); still data-not-behavior since it changes no wire bytes vs Enabled, only client interpretation (PRO-003 satisfied, PUSH-033). Synap keeps Enabled unchanged; Streaming adoption is a later optional internal upgrade.
- [x] 1.6 Author corpus vectors for the proposed push frames, loadable and marked proposal-stage per SPEC-005 conventions
      — conformance/vectors/proposal-push-streaming/ (5 vectors: data/progress/end/error/invalidate + README). Placed in a subdirectory because all five loaders glob vectors/*.yaml NON-recursively, so they are same-schema and promotable but out of the normative corpus (still 39, floor intact). frame_hex generated by the real thunder-go wire codec; each verified to decode-and-re-encode to identical bytes (round-trip proven during authoring), i.e. the envelope needs no wire-version bump — the proposal's central claim, checked not asserted.
- [x] 1.7 State the non-goals explicitly in the proposal: no implementation in this task; chunked streaming deferred to v2
      — 03-spec-001-push-amendment.md PUSH-040a..e: no implementation this task; chunked streaming is v2; no flow-control/credit protocol; no new command; Enabled is frozen.

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — the deliverable IS documentation: docs/analysis/push-streaming/ (README + 5 numbered files) plus the proposal-vectors README; indexed from docs/analysis/README.md (Related analyses).
- [x] 2.2 Write tests covering the new behavior — proposal-stage corpus vectors are the design's checkable artifact; each was round-trip-verified against the real thunder-go wire codec during authoring. No product code exists yet to unit-test (design-only task, 1.7/PUSH-040a); the implementation task that follows ratification owns the behavior tests.
- [x] 2.3 Run tests and confirm they pass — the 5 vectors decode-and-re-encode to identical bytes (Go wire); YAML schema-validated; the normative corpus is unaffected (top-level glob still 39, Go corpus green). No source changed, so the full multi-language gate is not re-triggered by this design pass.

## Note
Design-only task, complete except for 1.3 (Synap coordination) — a real, owner/cross-repo blocker.
Built ahead of the DAG's "depends on G5" sequencing preference, following the go-port precedent
(module built and corpus-verified before G5). The normative half — folding the amendment into
SPEC-001/002/003/004 and promoting the vectors — is mechanical once 1.3 lands (promotion checklist,
05-coordination-and-open-questions.md). Not archivable while 1.3 is open.
