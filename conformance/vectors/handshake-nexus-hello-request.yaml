name: handshake-nexus-hello-request
group: handshake
mode: bidirectional
frame_hex: "09 00 00 00 93 01 a5 48 45 4c 4c 4f 90"
decoded:
  kind: request
  id: 1
  command: HELLO
  args: []
notes: "Nexus AuthCommand handshake: the optional HELLO takes NO arguments and answers with a metadata Map {server, version, proto, id, authenticated} (nexus-server rpc/dispatch/admin.rs:54-77; canonical spec row 'HELLO | [] | Map'). This vector previously pinned a positional [Int(1)] payload - that is the RESP3 HELLO, a different surface, never the RPC one (BN-023 errata). Credentials never ride in HELLO under this profile; they travel via AUTH. Covers WIRE-004; shape pinned by SPEC-002 PRO-001."
