## 1. Implementation
- [x] 1.1 **DONE — `thunder-rpc` 0.1.0 is live on crates.io** (owner published 2026-07-17T12:52:42Z). **Checked first: `thunder` is TAKEN** — a 2018 CLI-boilerplate crate ("Create simple commandline apps with *zero* boilerplate!"), v0.3.1, 9,478 downloads, last published 2018-05-16. The name had never been checked; the whole spec assumed it. crates.io does not recycle published names and the squatter has a real download history, so **the T0.2-approved fallback `thunder-rpc` is applied, not re-litigated**. The collision is confined to the registry: `[package] name = "thunder-rpc"` + `[lib] name = "thunder"`, so `cargo add thunder-rpc` still gives `use thunder::…` and no product/SDK/doc/import changes. Internal path deps use the canonical rename form (`thunder = { path = "../thunder", package = "thunder-rpc" }`). Verified: workspace builds, corpus green, `cargo publish --dry-run` clean (25 files, 66.7 KiB). Recorded in SPEC-006 PKG-010/013/030 + the registry-names decision. Owner ran the publish; the crate resolves and the name question is settled
- [~] 1.2 **npm DONE — `@hivehub/thunder` 0.1.0 is live** (owner published 2026-07-17T12:53:42Z; tarball verified complete: ESM + CJS + `.d.ts`/`.d.cts` + README, Apache-2.0). The `@hivehub` scope caveat is therefore settled: the org exists and is ours. **PyPI `hivellm-thunder` and NuGet `HiveLLM.Thunder` remain the owner's to publish** — artifacts are built and validated locally (`hivellm_thunder-0.1.0{-py3-none-any.whl,.tar.gz}`, `twine check` PASSED; `HiveLLM.Thunder.0.1.0.nupkg`). One caveat still unresolvable from outside: NuGet **reserved ID prefixes** could reject `HiveLLM.*` despite the name being unpublished; it surfaces only at push
  - **Method note, worth keeping:** my first verification reported npm as *not published*. It was wrong. The WebFetch 404 was its 15-minute cache, and `npm view` then served npm's **own** cached 404 (`~/.npm/_cacache` stores negatives) — so "confirmed live, not cached" was itself a cached answer, and it grew into a false conclusion that the `@hivehub` org did not exist. `npm view --prefer-online` / `curl` return 200. **A negative result from a caching tool is not evidence of absence**; bypass the cache explicitly before reporting one
- [x] 1.3 Transplant `rpc-wire-format.md` v1 into `docs/spec/` verbatim + provenance header; coordinate a pointer note in the Nexus repo
- [x] 1.4 ~~Author `conformance/profiles/*.yaml` per PRO-001/PRO-011 (synap, nexus, vectorizer, lexum)~~ — **SUPERSEDED by `phase6_agnostic-config`** (owner: "nada de profiles nominais"). The four nominal YAMLs were replaced by a single `conformance/standard.yaml` carrying the same dimensions (handshake, hello style, push, caps, in-flight, error convention, TLS) with no product names and no `conformance/profiles/` directory. Scheme/port left to the app, since a library that knows product schemes is not agnostic (PRO-012)
- [x] 1.5 Verified against product source: **no HiveLLM product runs its binary RPC over TLS today** — not Vectorizer, not Synap, not Nexus (Lexum has no RPC transport at all). All three accept on a bare `TcpListener::bind` and hand the raw `TcpStream` to the frame loop, with no TLS accept path, no TLS config field, no secure scheme, and no cert material in any compose/k8s/env file. **Decision: SRV-040 keeps P1** — nothing in the family is waiting on it, so pulling it into M1 would be building for a deployment that does not exist. Implemented-but-optional remains right (`phase6_tls-optional`). **Vectorizer is a decoy and the reason this item existed:** `docs/specs/VECTORIZER_RPC.md:291-296` states "TLS: optional, controlled by `config.rpc.tls.cert_path`… the server wraps the TCP listener with `tokio-rustls`" — that config key does not exist, `RpcConfig` has only `{enabled, host, port}`, and the working rustls module (`security/tls.rs`) is reachable **only from tests** and negotiates ALPN `h2`/`http/1.1`, i.e. it was built for REST/gRPC. That spec section is aspirational, not shipped; Thunder must not inherit its claim (the same class of error as the BN-023 errata)
- [x] 1.6 Record every decision in `.rulebook/decisions/`

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — SPEC-006 PKG-010 now carries the availability column and the collision's reasoning (PKG-013/PKG-030 renamed with it); `.rulebook/decisions/2026-07-17-registry-names.md` records the `thunder`→`thunder-rpc` decision, the SRV-040 P1 decision, and the prepared owner commands
- [x] 2.2 Write tests covering the new behavior — no new behavior to test: the change is a registry identity plus a `[lib] name` that holds every existing import steady. The right test is the one that would catch it breaking, and it already exists — the whole suite imports `use thunder::…` and would fail to compile if the lib name moved. Guarded additionally by `cargo publish --dry-run`, which is what actually exercises the package name
- [x] 2.3 Run tests and confirm they pass — workspace builds; corpus green (`cargo test -p thunder-rpc --test corpus --test cross_decode`: 8); full Rust gate green (fmt + clippy `-D warnings` + 187 tests); `cargo publish --dry-run -p thunder-rpc` packages clean
